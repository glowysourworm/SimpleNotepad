<UserControl x:Class="SimpleNotepad.View.CodeTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SimpleNotepad.View"
             xmlns:localModel="clr-namespace:SimpleNotepad.Model"
             xmlns:wpfUtilView="clr-namespace:SimpleNotepad.View;assembly=SimpleNotepad">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <local:StringPropertyView StringName="Constraint" DataContext="{Binding Constraint}" />
        <local:StringPropertyView StringName="Variable" DataContext="{Binding Variable}" />
    </Grid>
</UserControl>